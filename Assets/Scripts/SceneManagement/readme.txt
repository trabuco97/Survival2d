-Depends on the saving system

[Workflow]
Each main game scene is defined in the IGameScene

The scene asset directed to it has to be named "<IGameScene.scene_name>_root"

Each subscene loaded alongside has to be named "<IGameScene.scene_name>_sub_<int>",
where is defined as a number

